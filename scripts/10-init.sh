#!/bin/bash
set -e

psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" <<-EOSQL
    CREATE USER $GNUMA_DB_USER WITH PASSWORD '$GNUMA_DB_PASS';
    CREATE DATABASE $GNUMA_DB_NAME;
    GRANT ALL PRIVILEGES ON DATABASE $GNUMA_DB_NAME TO $GNUMA_DB_USER;
EOSQL